{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://example.com/product.schema.json",
    "title": "Resolution",
    "description": "A Resolution from the Resolution Archive",
    "type": "object",
    "properties": {
        "meta": {},
        "title": {
            "type": "string",
            "description": "The title of the resolution"
        },
        "tag": {
            "type": "string",
            "description": "The tag for the resolution. (e.g. A1)"
        },
        "year": {
            "type": "integer",
            "description": "The year the resolution was passed"
        },
        "council": {
            "type": "string",
            "description": "The council that passed the resolution. Name or UUID"
        },
        "text": {
            "type": "string",
            "description": "The text of the resolution"
        },
        "status": {
            "type": "string",
            "description": "The status the resolution should be imported as",
            "enum": [
                "draft",
                "pending",
                "live",
                "archived"
            ]
        },
        "category": {
            "type": "string",
            "description": "The category of the resolution. Name or UUID"
        },
        "applicants": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "The applicants of the resolution. Names or UUIDs"
        }
    }
}
